name: API Tests

# Pipeline: roda o Health Check primeiro. Só se ele passar, dispara em paralelo
# as suites de Contract e Functional (ambas dependem do job healthcheck).
#
# Pré-requisito: configurar o secret AUTH_TOKEN no repositório
# (Settings > Secrets and variables > Actions) com um JWT válido.
# Os JWTs dessa API expiram em ~24h — renove o secret com
# ./scripts/refresh-ci-token.sh <jwt> antes de depender da pipeline.
# O job healthcheck valida o token antes de rodar os testes e falha rápido
# com mensagem clara se ele estiver vencido.
# O servidor goTest fica off em janelas de manutenção (dias úteis 23h–07h BR,
# fins de semana 08h–17h BR) — execuções nesses horários vão falhar com 503.

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  BASEURL: https://gotest.com.br/api/api
  DEFAULT_PROJECT_ID: b4a9db3b-9ef8-4734-99ed-d54dc1585803
  DEFAULT_SUITE_ID: 71c34269-e35e-4868-9518-9449e65b2da7
  AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
  LOG_ENABLED: "false"

jobs:
  healthcheck:
    name: Health Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'
          cache: maven
      - name: Verificar validade do AUTH_TOKEN
        run: |
          status=$(curl -s -o /dev/null -w "%{http_code}" "$BASEURL/users/me" -H "Authorization: Bearer $AUTH_TOKEN")
          if [ "$status" != "200" ]; then
            echo "::error::AUTH_TOKEN inválido ou expirado (GET /users/me retornou $status). Os JWTs dessa API expiram em ~24h — gere um token novo e rode ./scripts/refresh-ci-token.sh <jwt> para atualizar o secret."
            exit 1
          fi
          echo "AUTH_TOKEN válido."
      - name: Run Health Check suite
        run: mvn -B test -Phealthcheck
      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-healthcheck
          path: target/allure-results
          if-no-files-found: ignore

  contract:
    name: Contract
    needs: healthcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'
          cache: maven
      - name: Run Contract suite
        run: mvn -B test -Pcontract
      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-contract
          path: target/allure-results
          if-no-files-found: ignore

  functional:
    name: Functional
    needs: healthcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'
          cache: maven
      - name: Run Functional suite
        run: mvn -B test -Pfunctional
      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-functional
          path: target/allure-results
          if-no-files-found: ignore
